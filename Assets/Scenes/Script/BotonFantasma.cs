using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class BotonFantasma : MonoBehaviour
{
    /*
    private void OnCollisionEnter(Collision collision)
    {
        Modos mod = GameObject.FindObjectOfType<Modos>();
        if (collision.gameObject.tag == "Player")
        {
            if (mod.soyFantasma == true)
            {
                print("Boton fantasma desactivado");
            }

        }
    }

    private void OnCollisionStay(Collision collision)
    {
        Modos mod = GameObject.FindObjectOfType<Modos>();
        if (collision.gameObject.tag == "Player")
        {
            if (mod.soyFantasma == true)
            {
                print("Boton fantasma desactivado");
            }

        }

    }

    private void OnCollisionExit(Collision collision)
    {
        Modos mod = GameObject.FindObjectOfType<Modos>();
        if (mod.soyFantasma == true && collision.gameObject.tag == "Player")
        {
            print("Boton fantasma activado");
        }
    }*/
}
